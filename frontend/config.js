/* ══════════════════════════════════════════════════════════════════════
   CONFIG — edit this ONE file after you deploy the bot backend to Railway.
   Everything else (index.html, app.html) reads BACKEND_URL from here.
   ══════════════════════════════════════════════════════════════════════ */
const BACKEND_URL = "https://hf-v4-production.up.railway.app";

// Your bot's @username (no @, no https://), e.g. "hf_earn_bot".
// Used ONLY to build the "Open in Telegram" link shown when someone
// opens this Mini App outside of Telegram (e.g. pasted the link into a
// normal browser).
const BOT_USERNAME = "Hfearnmoneyv4bot";
